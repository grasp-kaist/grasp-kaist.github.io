{
  "name": "grasp-kaist.github.io",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "validate:profiles": "node scripts/validate-member-profiles.mjs",
    "test:profiles": "node --test tests/member-profile.test.mjs",
    "build": "npm run validate:profiles && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.4.6"
  },
  "devDependencies": {
    "ajv": "^8.20.0"
  }
}
